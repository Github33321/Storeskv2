# Если фронт и бек на одном домене — оставьте пустым.
# Если бек отдельно — укажите базовый URL (без /api).
# Пример: VITE_API_BASE=http://localhost:8082
VITE_API_BASE=

# Только для dev-сервера Vite: куда проксировать /api и /media
VITE_PROXY_TARGET=http://127.0.0.1:8082
